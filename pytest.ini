[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no network, no disk I/O)
    integration: Tests that combine multiple modules
    slow: Tests that are slow (LLM calls etc.)
